name: zhaiyana
description: A Flutter project for sports field booking and management
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  supabase_flutter: ^1.10.25
  table_calendar: ^3.0.9
  share_plus: ^7.2.2
  app_links: ^3.4.0        # ✅ заменили устаревший uni_links
  intl: ^0.20.2            # ✅ версия совместимая с SDK
  url_launcher: ^6.3.2
  webview_flutter: ^4.13.0
  uuid: ^4.5.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
